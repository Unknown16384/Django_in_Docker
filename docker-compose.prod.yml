services:
  web:
    build:
      context: ./app
      dockerfile: Dockerfile.prod
    command: gunicorn workspace.wsgi:application 0.0.0.0:8000
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
    expose:
      - 8000
    env_file:
      - ./.env.prod
    depends_on:
      - pgdb

  pgdb:
    image: postgres:17
    volumes:
      - pgdbdata:/var/lib/postgresql/data/
    env_file:
      - ./.env.prod

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
    ports:
      - 1337:80
    depends_on:
      - web

volumes:
  pgdbdata:
  static_volume:
  media_volume:
